/**
 * 运行时配置 —— 打包后可直接修改此文件切换后端地址，无需重新构建。
 * 部署后此文件位于 dist/config.js（与 index.html 同级）。
 * 修改后刷新浏览器即可生效（若被缓存，强刷一次）。
 */
window.__APP_CONFIG__ = {
  /**
   * 后端 API 基础地址。
   * - 留空 ''：走相对路径，适用于前端与后端同域、或由 Nginx 等反代 /api 到后端的部署方式。
   * - 指定地址：如 'http://192.168.1.10:9900'，适用于前后端分离、跨域访问（需后端开启 CORS）。
   */
  API_BASE_URL: '',
}
